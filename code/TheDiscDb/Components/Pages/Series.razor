@page "/series"

<PageTitle>All TV Series - TheDiscDb</PageTitle>
<HeadContent>
    <meta name="description" content="Browse the full list of TV series on TheDiscDb. Find Blu-ray, 4K UHD, and DVD disc details, MakeMKV playlists, and release information for every TV series in our database." />
    <link rel="canonical" href="https://thediscdb.com/series" />
</HeadContent>

<TheDiscDb.Client.Controls.Breadcrumb PrimaryText="TV Series" Items='[("Home", "/")]' />

<div class="page-header">
    <h1>All TV Series</h1>
    <div class="page-header__sub">Blu-ray, 4K UHD &amp; DVD releases</div>
</div>

@if (isCrawler)
{
    @if (ssrItems.Count == 0)
    {
        <p>No TV series have been added yet.</p>
    }
    else
    {
        <div class="media-item-list">
            @foreach (var item in ssrItems)
            {
                <a href="@item.ItemDetailUrl()" class="media-item-card" title="@item.Title">
                    <MediaImage Url="@item.ImageUrl" Width="156" Height="231" Title="@item.Title" />
                    <span class="media-item-label">@item.Title (@item.Year)</span>
                </a>
            }
        </div>
    }
}
else
{
    <TheDiscDb.Client.Pages.Series @rendermode="new InteractiveWebAssemblyRenderMode(prerender: false)" />
}
