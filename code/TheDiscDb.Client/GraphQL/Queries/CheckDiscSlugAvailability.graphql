query CheckDiscSlugAvailability($tmdbId: String, $discSlug: String) {
  mediaItems(
    first: 1
    where: {
      and: [
        { externalids: { tmdb: { eq: $tmdbId } } }
        {
          releases: {
            some: {
              discs: { some: { slug: { eq: $discSlug } } }
            }
          }
        }
      ]
    }
  ) {
    nodes {
      slug
    }
  }
}