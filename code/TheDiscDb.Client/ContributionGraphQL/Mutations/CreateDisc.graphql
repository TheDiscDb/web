mutation CreateDisc($input: CreateDiscInput!) {
  createDisc(input: $input)
  {
    userContributionDisc {
      encodedId
    }
    errors {
      code: __typename
      ... on Error {
        message
      }
    }
  }
}