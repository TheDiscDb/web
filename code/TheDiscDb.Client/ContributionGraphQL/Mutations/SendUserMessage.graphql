mutation SendUserMessage($input: SendUserMessageInput!) {
  sendUserMessage(input: $input) {
    userMessage {
      id
      contributionId
      fromUserId
      toUserId
      message
      isRead
      createdAt
      type
    }
    errors {
      code: __typename
      ... on Error {
        message
      }
    }
  }
}
