mutation RemoveDiscFromBoxset($input: RemoveDiscFromBoxsetInput!) {
  removeDiscFromBoxset(input: $input) {
    userContributionBoxset {
      encodedId
    }
    errors {
      code: __typename
      ... on Error {
        message
      }
    }
  }
}
