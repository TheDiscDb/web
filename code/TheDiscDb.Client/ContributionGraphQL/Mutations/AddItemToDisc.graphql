mutation AddItemToDisc($input: AddItemToDiscInput!) {
  addItemToDisc(input: $input) {
    userContributionDiscItem {
      encodedId
    }
    errors {
      code: __typename
      ... on Error {
        message
      }
    }
  }
}
