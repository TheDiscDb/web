mutation HashDisc($input: HashDiscInput!) {
  hashDisc(input: $input) {
    discHash {
      hash
      fingerprint
    }
    errors {
      ... on ContributionNotFoundError {
        message
      }
    }
  }
}
