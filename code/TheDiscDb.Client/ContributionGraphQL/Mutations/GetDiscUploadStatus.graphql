mutation GetDiscUploadStatus($input: DiscUploadStatusInput!) {
  discUploadStatus(input: $input)
  {
    discUploadStatus {
      logsUploaded
      logUploadError
    }
    errors {
      code: __typename
      ... on Error {
        message
      }
    }
  }
}