query ContributionDiscs($contribtionId: EncodedIdFilter) {
  myContributions(where:  {
     encodedId:  {
        eq: $contribtionId
     }
  })
  {
    nodes {
      encodedId
      year
      releaseTitle
      releaseSlug
      status
      mediaType
      releaseDate
      regionCode
      title
      asin
      upc
      externalProvider
      externalId
      frontImageUrl
      backImageUrl
      created
      locale
      discs {
        encodedId
        index
        name
        existingDiscPath
        logsUploaded
        format
        slug
      }
    }
  }
}