query GetMessageThreads {
  messageThreads {
    contributionId
    encodedContributionId
    contributionTitle
    mediaTitle
    lastMessagePreview
    lastMessageAt
    unreadCount
    totalCount
  }
}
