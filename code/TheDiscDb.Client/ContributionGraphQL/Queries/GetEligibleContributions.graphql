query GetEligibleContributions($first: Int, $after: String) {
  myContributions(first: $first, after: $after) {
    nodes {
      encodedId
      title
      year
      releaseTitle
      mediaType
      status
      frontImageUrl
      discs {
        encodedId
        name
        format
        slug
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
