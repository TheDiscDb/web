query GetCurrentUserContributions($where: UserContributionFilterInput) {
  myContributions(where: $where) {
    nodes {
      encodedId,
      title
      releaseTitle
      year
      mediaType,
      status,
      frontImageUrl,
      created
    }
  }
}